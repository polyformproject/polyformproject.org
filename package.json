{
  "name": "polyformproject.org",
  "private": true,
  "packageManager": "npm",
  "engines": {
    "node": "^25.9.0"
  },
  "devDependencies": {
    "commonmark": "^0.31.2",
    "ejs": "^5.0.2",
    "escape-html": "^1.0.3",
    "gray-matter": "^4.0.3",
    "licensee": "^12.0.1",
    "rss": "^1.2.2",
    "semver": "^7.7.4"
  },
  "scripts": {
    "build": "./build",
    "postbuild": "find site -iname '*.html' -exec tidy -modify -quiet -indent --indent-spaces 2 --tidy-mark no --wrap 0 --add-meta-charset --gnu-emacs yes --drop-empty-elements no --fix-uri no --force-output yes {} \\;",
    "licenses": "licensee --errors-only"
  }
}
